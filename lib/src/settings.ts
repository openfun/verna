// Char used by RJSF to join the path of a field to convert it as an id
export const RJSF_ID_SEPARATOR = '_';

// The supported translations for the application
export const VERNA_SUPPORTED_LOCALES = ['en-US', 'fr-FR', 'fr-CA', 'es-ES'] as const;
